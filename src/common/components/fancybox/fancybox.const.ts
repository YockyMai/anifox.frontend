import { OptionsType } from '@fancyapps/ui/types/Fancybox/options'

export const DEFAULT_DELEGATE_SELECTOR = '[data-fancybox]'
export const DEFAULT_DELEGATE_VALUE = 'gallery'

export const DEFAULT_FANCYBOX_OPTIONS: Partial<OptionsType> = {
  Carousel: {
    infinite: false
  }
}
