mutation ToggleFavoriteCharacter($characterId: String!) {
  toggleFavoriteCharacter(characterId: $characterId) {
    userId
    user {
      id
      name
    }
    characterId
    character {
      id
    }
  }
}
