query FriendInvitations(
  $userId: String!
  $search: String
  $limit: Int
  $page: Int
) {
  friends(userId: $userId, search: $search, limit: $limit, page: $page) {
    data {
      ...Friendship
    }
    pageInfo {
      hasNextPage
      page
    }
  }
}
