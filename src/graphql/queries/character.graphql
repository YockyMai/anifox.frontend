query Character($characterId: String!, $userId: String) {
  character(id: $characterId) {
    about
    aboutEn
    id
    image
    malId
    name
    nameEn
    nameKanji
    pictures
    isFavorite(userId: $userId)
  }
}
